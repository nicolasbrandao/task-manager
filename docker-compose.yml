version: "3.8"

services:
  app:
    build:
      context: .
    ports:
      - "8080:8080" # backend
      - "5173:5173" # frontend
    volumes:
      - .:/app
      - /app/backend/node_modules
      - /app/frontend/node_modules